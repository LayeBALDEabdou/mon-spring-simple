services:
  - type: web
    name: mon-spring-app
    env: java
    buildCommand: ./mvnw clean install -DskipTests
    startCommand: java -jar target/*.jar
    envVars:
      - key: SERVER_PORT
        value: 10000
    plan: free